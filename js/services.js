// JavaScript specific to services-sellers.html

// Инициализация всех обработчиков для services-sellers.html
document.addEventListener('DOMContentLoaded', function() {
    // Обработка форм
    handleForm("feedbackFormTop", "Обратная связь (верхняя)");
    handleForm("feedbackFormBottom", "Обратная связь (нижняя)");
    
    // Дополнительная инициализация для страницы услуг
    console.log("Services page initialized");
});
